# ── Environnement Python ────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# ── Secrets & configuration locale ──────────────────────────────────────────
backend/.env
frontend/.env
.env

# ── LaTeX — artefacts de compilation ────────────────────────────────────────
# Garder uniquement les sources .tex et les PDF compilés
rapports/**/*.aux
rapports/**/*.log
rapports/**/*.fls
rapports/**/*.fdb_latexmk
rapports/**/*.synctex.gz
rapports/**/*.toc
rapports/**/*.out
rapports/**/*.thm
rapports/**/*.lof
rapports/**/*.lot
rapports/**/*.bbl
rapports/**/*.blg
rapports/**/*.idx
rapports/**/*.ind
rapports/**/*.ilg
rapports/**/*.dvi
rapports/**/pdflatex*.fls

# ── Données ─────────────────────────────────────────────────────────────────
backend/data/*.db
backend/data/*.db-shm
backend/data/*.db-wal

# ── Divers ───────────────────────────────────────────────────────────────────
.DS_Store
*.pem
